# FileLocker

Windows 檔案／資料夾保護工具。提供三種互相獨立、安全等級與保護對象都不同的保護機制：**加密**（真正的密碼學保護，內容經 AES-256-GCM 加密後移入 Vault）、**資料夾防護**（純 Windows ACL 存取限制，內容不加密、原地保留）、**密碼庫**（Password Locker，保存網站帳密與已加密檔案密碼，不保護檔案本身）。

## Language

**加密（Encrypt）**：
把檔案／資料夾內容用 Argon2id 衍生金鑰 + AES-256-GCM 加密。同一套密碼學保護底下有兩種產出物存放模式——**集中庫加密**（預設）與**單檔案分散式加密**（見下方兩則詞條，已實作）——兩者共用同一套密碼／Passkey／恢復金鑰解鎖機制，差別只在加密內容放哪裡、要不要留 `.locked` 指標檔。
_Avoid_: 鎖定、上鎖（這兩個詞在 UI 上保留給下面的「資料夾防護」使用，加密功能對使用者只稱「加密／解密」，不稱「鎖定」）

**集中庫加密（Vault Encryption）**：
「加密」的預設存放模式（既有、唯一已實作的行為）：內容移至 Vault 集中管理，原位置留下 `.locked` 指標檔。這是系統既有、最強的保護等級——內容離開持有者電腦（例如硬碟被拆走）依然無法被讀取。

**單檔案分散式加密（Standalone File Encryption）**：
「加密」的另一種存放模式，使用者在單次加密操作時勾選啟用（UI 顯示為「獨立加密」），不是全域設定。加密內容不移入 Vault，預設原地取代（原始明文刪除，加密後的 `.flocked` 檔案就留在原本的位置），使用者也可以在加密過程中另外勾選「存放到其他地方」改變落腳位置。不論輸入是單一檔案還是資料夾，產出永遠是單一顆 `.flocked` 加密檔（資料夾一樣先壓縮成一份再加密，封裝方式跟集中庫模式相同，只有落腳位置跟副檔名不同）。內部程式碼命名（`StorageMode.Standalone` 等）沿用這個詞彙，跟 UI 顯示字「獨立加密」不必逐字對應（見 [`單檔案分散式加密_功能規劃.md`](docs/specs/features/單檔案分散式加密_功能規劃.md) 第 3 節）。

**Vault**：
集中庫加密內容存放的管理區，只有「集中庫加密」模式會寫入；單檔案分散式加密不使用 Vault。資料夾防護也不使用 Vault。

**`.locked` 指標檔**：
集中庫加密後留在原位置的指標檔，內容經 HMAC-SHA256 簽章，本身不含加密內容（真正的密文在 Vault 裡的 `{uuid}.enc`）。這個副檔名跟機制專屬於集中庫加密模式，資料夾防護不產生 `.locked` 檔案（防護中的資料夾原地保留，不會被搬走或替換成指標檔）。

**`.flocked` 獨立密文檔**：
單檔案分散式加密的產出檔，副檔名刻意跟 `.locked` 同一家族但不同種——跟指標檔不同，這顆檔案本身就背著完整密文（沒有 Vault 可以指過去），檔案大小跟原始內容同等級，不是幾百 bytes 的小檔案。副檔名本身要唯一，讓 App 雙擊時能直接判斷「這是指標檔（去 Vault 查）」還是「這是獨立密文檔（直接原地解密）」，不用額外解析檔案內容才知道類型。

**資料夾防護（Folder Guard）**：
透過拒絕目前 Windows 帳號在該資料夾上的 ACL 權限來限制存取，不加密內容、資料夾原地保留不搬動、不需要提權（見 [ADR-0001](docs/adr/0001-folder-guard-deny-acl-not-ownership-transfer.md)）。是比「加密」弱、刻意接受的保護等級——防的是「不小心/隨手打開」，不是「蓄意繞過權限」或「離線讀取硬碟」；擁有者本人隨時能自行改回權限，不需要密碼。獨立分頁，跟「加密」分頁區隔，統一管理所有上鎖中的資料夾。
_Avoid_: 鎖定資料夾（跟既有「鎖定＝加密」語彙衝突）、限制存取（备选但未採用）

**資料夾防護密碼／Passkey**：
整個「資料夾防護」功能共用同一組（不是每個資料夾各自一組，跟「加密」分頁的每項目獨立密碼不同）。密碼必填（第一次上鎖任何資料夾前強制設定），Passkey 選配、密碼永遠是保底解鎖手段。清單頁可個別解鎖單一資料夾，或用獨立的「解鎖全部」按鈕一次解鎖所有項目（需二次確認）。重設密碼／Passkey 直接套用到所有正在上鎖中的資料夾，不需要先解鎖——因為這組憑證只是身份驗證雜湊，不是解鎖資料夾內容用的鑰匙（ACL 拒絕規則本身不是從密碼算出來的）。即使密碼與 Passkey 都遺失，使用者仍能透過 Windows 內建的檔案總管「安全性」設定拿回資料夾存取權，這點會在設定頁主動告知，跟「加密」的「密碼遺失＝無法復原」是刻意做出的差異。

**上鎖／解鎖（Lock / Unlock）**：
「資料夾防護」分頁內的動作動詞，指設定／解除 ACL 限制。這兩個字只在資料夾防護分頁使用；加密功能的對應動作固定稱「加密／解密」，兩邊動詞完全不共用，避免使用者混淆兩種保護等級。

**密碼庫（Password Locker）**：
第三種保護機制，跟「加密」「資料夾防護」都獨立：儲存網站帳密與已加密檔案的密碼，本身不保護檔案或資料夾。英文名稱用 Locker，不用 Vault，因為 Vault 是加密功能專屬的集中存放區，密碼庫是完全獨立的另一份儲存、不寫入 Vault，沿用 Vault 這個字會誤導使用者以為兩者存在同一個地方。驗證方式獨立於加密（密碼／Passkey／恢復金鑰）與資料夾防護（共用密碼／Passkey）之外，是第三套各自獨立的憑證。
_Avoid_: 密碼管理員、Password Vault（撞到既有 Vault 專有名詞）

**驗證（Verify）**：
密碼庫進入可存取狀態的動作動詞。不用「上鎖／解鎖」（資料夾防護專用）、也不用「加密／解密」（加密功能專用），因為借用其中任一組都會讓使用者把密碼庫的心智模型跟另外兩種保護機制混在一起。
_Avoid_: 解鎖密碼庫、登入密碼庫

**憑證（Credential）**：
密碼庫裡的一筆紀錄，欄位為帳號、密碼、關聯網站（可多個）、備註。分類拆成兩個獨立欄位（PasswordVault 獨立化規劃定案，見下面「類別標籤」「已加密檔案憑證」「PasswordVault」）：`CategoryLabel`（自由文字分組標籤）與 `IsEncryptedFile`（布林值，決定要不要走 Vault 連結那套特殊行為）——不是單一的固定 enum。「已加密檔案」類的憑證會關聯到一個 Vault 項目，該項目被刪除或解密後，憑證不會被刪除，但標題顯示刪除線並標示來源已消失。

**類別標籤（Category Label）**：
憑證的自由文字分組欄位，使用者新增憑證時直接打字輸入，相同名稱自動歸為同一類，不需要另外一套「建立類別」的管理介面。「網站」不再是寫死的特殊值，只是系統預設帶入的其中一個標籤字串，使用者可以改成任何名稱（例如「銀行」「軟體授權金鑰」）。除了 `IsEncryptedFile=true` 的憑證，其餘標籤底下的憑證行為完全同等——都支援關聯網域、瀏覽器自動填入、TOTP，不因為標籤名稱不同而有差異。
_Avoid_: 分類（Category）單獨指這個欄位——「分類」這個詞現在同時牽涉 CategoryLabel 跟 IsEncryptedFile 兩個獨立欄位，容易只講到一半。

**已加密檔案憑證（Encrypted-File Credential）**：
`IsEncryptedFile=true` 的憑證——唯一行為跟其他憑證不同的一種：關聯到一個 Vault 項目（或手動新增、不連結任何 Vault 項目）、不支援自動填入／網域關聯／TOTP、PasswordVault 獨立版預設隱藏這一類，只有偵測到同一台電腦也裝了 FileLocker 主體才會顯示。

**PasswordVault**：
密碼庫功能獨立化後的品牌名稱與獨立桌面應用程式（`PasswordVault.exe`），可以不依賴 FileLocker 主體單獨運作；核心邏輯（原 `FileLocker.PasswordLocker`，改名 `PasswordVault.Core`）與這個新程式的原始碼都遷出到獨立 GitHub repo，是該功能的唯一真相來源，這個 FileLocker repo 之後只透過既有的「下載外掛部件」機制取得編譯好的二進位檔。FileLocker 本體 UI 上「密碼庫」這個分頁名稱維持不變，改名範圍是專案／程式碼／安裝路徑層級，不要求使用者跟著改口。詳見 [`PasswordVault_獨立化_規劃.md`](docs/specs/features/PasswordVault_獨立化_規劃.md)、[ADR-0003](docs/adr/0003-passwordvault-separate-repo.md)。
_Avoid_: 直接用「密碼庫」指稱這個獨立程式本身——「密碼庫」在 FileLocker 語境裡專指 App 內的那個分頁，PasswordVault 是可以脫離 FileLocker 存在的獨立產品，兩者不是同一個指稱範圍。

**選擇密碼（Choose Password）**：
讓使用者從密碼庫已存的憑證裡挑一筆重複使用的機制，不限瀏覽器情境——瀏覽器擴充功能裡是「偵測不到目前網站對應的既有憑證時（例如註冊頁）」透過 popup 跳出清單；App 內對應的是獨立工具列按鈕「關聯到現有帳號」（不在「新增帳密」表單裡，兩者是不同的操作，見規劃文件第 11.5 節），兩處都是同一個底層機制：不建立新紀錄，直接把目前網站併入被選中那筆既有憑證的關聯網站清單（`AssociatedDomains`），不會靜默自動關聯——瀏覽器端一定會先用該筆紀錄自己既有的網域觸發一次驗證，才把新網域併進去。

**分頁內驗證狀態**：
密碼庫分頁自己的「本次已驗證」狀態——驗證一次（顯示/複製/刪除任一筆）之後，逾時分鐘數內對分頁中任何一筆的後續動作都不用再驗證，不分網站，整個分頁共用一個計時器。跟瀏覽器擴充功能的「每網站獨立計時、滑動視窗」（見規劃文件第 3 節）是同一個逾時分鐘數設定值，但作用範圍不同：前者是「App 內使用者在管理自己整份清單」的情境，後者是「瀏覽器裡逐一造訪不同網站」的情境，不能共用同一份執行期狀態。

**可選配部件（Optional Component）**：
不隨 FileLocker 主體安裝程式一起裝上、使用者另外安裝的獨立模組，目前唯一的例子是整個密碼庫功能。技術上是一個編譯期不被 FileLocker.App 依賴的獨立 dll，執行期偵測固定資料夾裡有沒有這個檔案來判斷「有沒有裝」，透過極簡的介面契約動態載入（見規劃文件第 2 節）。跟「外掛」「插件」是同一個概念，這個專案統一用「部件」。
_Avoid_: 外掛、插件、模組（跟一般程式碼模組化的「模組」混淆）

**CLI 獨立發布產物（CLI_setup／CLI_zip）**：
規劃中，尚未實作。除了完整 GUI 安裝程式內附的 `cli/` 子資料夾（見「CLI 隨裝發布」）之外，另外提供兩顆只含 CLI 的獨立發布產物：`CLI_setup`（`FileLocker_CLI_vX.Y.Z_setup.exe`，用 mswi-cli 打包，`no_admin_install: true`、`dependencies: ["dotnet_desktop"]`、獨立的 `app_name`/`folder_name`「FileLocker CLI」，不設定 `file_associations`）與 `CLI_zip`（`FileLocker_CLI_vX.Y.Z_portable.zip`，`cli/` 資料夾內容原封不動＋一份簡短的 `README.md` 說明如何手動加入 PATH）。兩者內容都直接沿用 `dotnet publish src/FileLocker.App` 順便產出的 `publish/cli/` 資料夾（本身已自足，不依賴 GUI 那份輸出），不另開獨立 build 流程；版本號固定跟主程式同一個，不單獨編號——這兩個產物永遠只會在跟主程式同一次 release 裡一起產生。`app_name`/`folder_name` 特意跟 GUI 版（`FileLocker`）分開，避免同時裝兩者時安裝目錄/解除安裝登記互相覆蓋；PATH 裡因此重複出現兩份 `FileLocker.Cli.exe` 是接受的結果，不特別處理。第一輪先手動打包驗證，不寫進 `/release` skill 的自動化流程，確認沒有意外的坑之後再考慮收進去。
_Avoid_: 把這兩個產物跟「部件」混為一談——部件是執行期動態載入、影響 FileLocker.App 功能完整度的模組，CLI_setup／CLI_zip 單純是同一份 CLI 程式碼的另一種發布形式，不影響任何功能行為。