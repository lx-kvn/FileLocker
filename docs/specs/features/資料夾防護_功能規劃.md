# FileLocker「資料夾防護」功能規劃

**狀態：已完成實作，併入《技術規格文件》第 21 節**（本文件保留作為原始設計訪談紀錄，記錄當初定案理由；目前實際行為與狀態以技術規格文件第 21 節為準，兩者有出入時以技術規格文件為準）

配套文件：[`CONTEXT.md`](../../../CONTEXT.md)（術語）、[`docs/adr/0001-folder-guard-deny-acl-not-ownership-transfer.md`](../../adr/0001-folder-guard-deny-acl-not-ownership-transfer.md)（機制與威脅模型的取捨紀錄）

---

## 1. 這是什麼

「資料夾防護」（Folder Guard）是獨立於「加密」的第二種保護機制：**不加密內容**，純粹透過 Windows ACL 拒絕目前登入帳號對某資料夾的存取權，資料夾原地保留、不搬動、不需要提權。定位是「防隨手瀏覽」，不是「防蓄意繞過」——威脅模型與機制選擇的完整推理見 ADR-0001。

跟「加密」分頁刻意保持語彙區隔：加密用「加密／解密」，資料夾防護用「上鎖／解鎖」（`Lock`／`Unlock`），兩邊動詞互不共用，避免使用者混淆兩種保護等級的差異（見 `../../../CONTEXT.md`）。

## 2. 範圍

- **只支援資料夾，不支援單一檔案**（v1）。ACL 機制本身檔案/資料夾通用，之後要擴充到檔案是低成本的事，但先不做。
- **CLI 不支援**（v1）。這個功能是桌面互動導向（右鍵、臨時保護），沒有像加密那樣明確的無人值守使用情境；而且會特地去用命令列的使用者，本來就有能力用 `icacls` 之類工具直接繞過 ACL 拒絕規則，CLI 支援對這個定位的使用者沒有實質意義。

## 3. 憑證模型

- **整個「資料夾防護」功能共用一組密碼＋選配 Passkey**，不是每個資料夾各自一組（跟「加密」的每項目獨立密碼不同）。第一次上鎖任何資料夾前，強制先完成這組共用憑證的設定。
- **密碼必填，Passkey 選配**，密碼永遠是保底解鎖手段——不能讓 Passkey 變成唯一解鎖方式，否則 Passkey 失效（換裝置、憑證損壞）就永久卡死，而這個功能沒有像加密那樣的「恢復金鑰」機制可以兜底。
- **密碼錯誤鎖定**：套用跟加密一樣的機制（`LockoutTracker`，連續錯 5 次、指數退避最長 1 小時），但鍵值不是每個項目的 UUID，而是固定用一個代表整個功能的常數鍵（例如 `"folder-guard"`）——代表鎖定會影響「所有」正在上鎖的資料夾，這是刻意接受的取捨（暴力猜測防護的價值大於「鎖定期間連自己都解不開」的副作用，跟加密密碼鎖定機制的既有邏輯一致）。
- **Passkey 略過鎖定機制**，跟現有「密碼鎖定機制不適用於 Passkey」的邏輯一致（規格文件第 6.4 節）——TPM 硬體驗證沒有能用軟體反覆嘗試的「猜」的環節。
- **重設密碼／Passkey**：直接套用到所有正在上鎖中的資料夾，不需要先解鎖現有項目——因為這組憑證只是身份驗證雜湊，不是解鎖資料夾內容用的鑰匙，ACL 拒絕規則本身不是從密碼算出來的。
- **忘記密碼與 Passkey 都失效的情境**：使用者仍能透過 Windows 內建的檔案總管「內容→安全性→進階」拿回資料夾存取權（擁有者永遠拿得回控制權，見 ADR-0001）。這件事要在設定頁主動告知使用者，作為跟「加密」的「密碼遺失＝無法復原」的刻意對比，而不是留給使用者自己踩雷才發現。

## 4. ACL 機制細節

- 對目前登入帳號的 SID 加上拒絕 `ReadAndExecute`／`Write`／`Delete` 的 ACE。
- 不處理父層列舉權限、不搭配隱藏屬性——資料夾在檔案總管裡看得到，雙擊進去才會被拒絕（Windows 原生「存取被拒」錯誤視窗，不攔截、不替換成自訂畫面，見 ADR-0001）。
- 拒絕 `Delete` 權限連帶擋住重新命名（NTFS 底下重新命名一個物件需要對該物件本身的 `Delete` 權限）。

## 5. Shell Extension 整合

- 現有 `FileLockerShellExtension.dll` 只插入一個「使用 FileLocker 加密」選單項目，`InvokeCommand` 目前寫死只接受單一命令 id。需要擴充成兩個命令 id（0=加密，1=上鎖），並且讓啟動 `FileLocker.App.exe` 的命令列能標示這次要開哪個分頁／執行哪個動作。
- **「上鎖」選單項目只在選取的項目全部是資料夾時才出現**——選取中只要混到任何一個檔案，就不顯示這個選項，不做「自動忽略檔案只鎖資料夾」這種隱性行為。
- **支援多選批次上鎖**：因為憑證是共用一組，批次上鎖不需要處理加密批次的複雜度（第 4.5 節那種「多選時 Passkey/恢復金鑰勾選框鎖住」的問題），純粹對每個選取的資料夾各自套用同一組 ACL 拒絕規則。

## 6. 上鎖互動

- **已設定過共用密碼**：右鍵點「上鎖」**不開啟主程式**，跳出一個原生 WPF 小視窗（比照 `PasswordPromptWindow` 的技術——不透過 WebView2，讓視窗盡快跳出來），內容「你要將『OO』上鎖嗎？」＋確定／取消按鈕，Esc 可退出。**上鎖本身不需要輸入密碼**（密碼只用來驗證解鎖身份，不是上鎖的必要條件），確認彈窗本身已經足夠防止手滑誤觸。完成後跳右下角通知，不需要開啟主程式。
  - 小視窗下方有一行小字＋連結按鈕：「此功能並非加密，欲加密請至加密頁」，點下去會開啟／喚起主程式，跳到「加密」分頁步驟一，並把這次選取的項目直接帶入清單。
- **尚未設定過共用密碼**：右鍵點「上鎖」改成開啟主程式、跳到「資料夾防護」分頁，引導完成首次設定，設定完成後才真的上鎖這次選取的資料夾。

## 7. 解鎖情境

解鎖依觸發來源分成兩類，行為不同（見第 9 節清單頁行為）：

### 7.1 資料夾防護分頁清單頁（主程式內操作）

- 可針對單一項目按「解鎖」。
- 也可以用獨立的「解鎖全部」按鈕，一次解鎖清單中所有正在上鎖的項目，按下去先跳確認彈窗。
- 兩種情況都是：先看有沒有設定 Passkey，有的話直接跳出 Windows Hello 驗證；使用者把驗證視窗關掉才退回密碼輸入畫面（比照規格文件 14.4 節 `PasswordPromptWindow` 遇到 Passkey 項目時的既有互動模式）。

### 7.2 臨時彈窗解鎖（不透過清單頁）

兩種觸發情境，UX 一致（Passkey 優先、退回密碼）：

1. **雙擊被拒絕的資料夾**：跳出「此資料夾已鎖定，你要解鎖此資料夾嗎?」的彈窗（原生 WPF 小視窗，跟第 6 節上鎖確認彈窗同一套技術），提供取消／解鎖按鈕。
2. **加密流程撞到巢狀防護中的資料夾**（見第 8 節）：跳出彈窗列出被擋的子資料夾清單，提供取消／解鎖按鈕。

## 8. 與加密流程的互動

ACL 拒絕規則掛在目前登入帳號的 SID 上，FileLocker App 自己的行程也是用同一個帳號跑，**加密流程讀取被上鎖的資料夾時一樣會被拒絕存取**，不只是使用者在檔案總管點不進去而已。

處理方式：加密流程（`FolderArchiver` 掃描壓縮）掃描到資料夾防護中的子資料夾時，中止當下的加密動作，跳出彈窗：「OO資料夾內含正在上鎖的項目（子資料夾：XXX、XXX），你要解鎖這幾個資料夾嗎？」，提供取消／解鎖按鈕。按解鎖後：

1. 若已設定 Passkey，直接跳出 Windows Hello 驗證；未設定則要求輸入共用密碼。
2. 驗證通過後解鎖列出的所有巢狀防護資料夾（一次性，因為是共用憑證），然後**接續原本的加密流程**。
3. 這些資料夾一旦被加密流程消耗（打包進外層 zip，原始子資料夾隨外層資料夾一起被刪除），它們在資料夾防護清單／索引裡對應的項目也要一併清除——資料夾本身已經不存在了，繼續留著索引只會變成指向不存在路徑的殘留紀錄。

## 9. 清單頁行為（「資料夾防護」分頁）

- 顯示所有目前被防護中、以及「已在分頁內解鎖但使用者還沒手動刪除」的項目。
- **狀態**：`上鎖中` / `已解鎖`。
- 透過清單頁解鎖（第 7.1 節）的項目：狀態變成「已解鎖」，繼續留在清單裡，使用者可以手動點刪除按鈕把它從清單移除。
- 透過臨時彈窗解鎖（第 7.2 節）的項目：**不留記錄**，直接從清單消失（因為觸發情境是「臨時需要解決眼前的操作」，不是分頁內的管理行為）。
- **健壯性檢查**：清單頁載入時，針對索引裡記錄的每個路徑即時檢查該路徑目前的 ACL 是否真的有對應的拒絕規則（比對 SID）。不符合的話視為「已不在防護中」，直接從清單移除或標記異常，不照單全收索引檔內容——比照 `VaultManager.ScanAll()` 跟 `MarkerStatusChecker` 現有「以磁碟實際狀態為準，索引只是加速用途」的設計原則（規格文件 10.1、10.3 節）。

## 10. 設定頁整合

比照現有「關鍵操作驗證」（規格文件 14.3、8.1 節）的模式，新增一組「資料夾防護」管理區塊：設定／重新設定密碼、設定／重新設定／停用 Passkey。文案需要包含第 3 節提到的「忘記密碼還是拿得回存取權」說明。

## 11. 資料儲存（初步設計，實作時可微調）

- 索引與憑證資料獨立於 Vault 之外（這個功能完全不用 Vault）：`%LocalAppData%\FileLocker\FolderGuard\guarded-folders.json`，原子寫入，比照 `settings.json` 慣例。
- 內容大致包含：共用密碼的驗證雜湊＋鹽值（Argon2id，只需要驗證用途，不需要衍生加密金鑰）、Passkey 設定狀態與憑證名稱（比照 `CriticalActionCredentialName` 模式）、被防護資料夾清單（路徑、狀態、時間戳）。
- C# 服務類別命名避開 `Lock` 字樣（例如 `FolderGuardService`），避免跟既有 `LockService`（加密流程）混淆。

## 12. 待補：新增的多語言與錯誤代碼

- `locales/zh-TW.json`、`en.json` 新增資料夾防護分頁的所有文案 key（沿用現有 `t()` 慣例）。
- `ErrorCodes` 新增資料夾防護專屬的錯誤代碼（例如「尚未設定共用密碼」「密碼錯誤」「鎖定中」「選取項目包含檔案」「路徑不是資料夾」等），沿用現有 `ErrorCode` + `ErrorDetail` 雙欄位設計（規格文件第 17 節）。

## 13. 已知會延後或不做的事（跟 ADR-0001 呼應，避免之後被誤改）

- 不做擁有權轉移／隱藏帳號機制（防蓄意繞過），機制與理由見 ADR-0001。
- 不攔截雙擊資料夾的原生錯誤畫面，也不做 Windows 物件存取稽核（Security Event Log）監聽機制——技術上可行但需要提權設定＋新增背景常駐服務，成本跟這個功能「防隨手瀏覽」的定位不成比例（ADR-0001 補述）。
- v1 不支援單一檔案、不支援 CLI（第 2 節）。